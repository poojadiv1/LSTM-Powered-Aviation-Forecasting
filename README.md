# LSTM-Powered-Aviation-Forecasting
LSTM-based time series forecasting system that predicts monthly airline passenger demand using a 12-month historical lookback window. The project applies data scaling, sliding-window lag features, and deep learning with LSTM networks, with forecasting performance evaluated using RMSE.
